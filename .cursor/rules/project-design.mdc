---
description: Financial domain relationships, account access, balances, transfers, syncs, and provider architecture.
globs: app/models/**/*.rb, app/jobs/**/*.rb, db/**/*.rb
alwaysApply: false
---

Read and follow the shared architecture guide when changing these paths. This rule only controls Cursor's file-scoped loading; maintain the guidance in the shared document.

@docs/llm-guides/architecture.md
