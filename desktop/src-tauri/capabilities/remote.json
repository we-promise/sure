{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "remote-bridge",
  "description": "Minimal IPC granted to the remote Sure server pages loaded in the main webview: drag the window, the event bridge (notifications/badge), opening the prefs window, and starting SSO in the system browser. No filesystem/shell/HTTP access. You only ever point the app at your own Sure server, which already holds your data.",
  "windows": ["main"],
  "remote": {
    "urls": ["https://*/**", "http://*/**", "https://*", "http://*"]
  },
  "permissions": [
    "core:window:allow-start-dragging",
    "core:window:allow-show",
    "core:window:allow-set-focus",
    "core:event:allow-emit",
    "core:event:allow-listen",
    "notification:default"
  ]
}
