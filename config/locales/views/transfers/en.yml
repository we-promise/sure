---
en:
  transfers:
    create:
      success: Transfer created
    destroy:
      success: Transfer removed
    form:
      amount: Amount
      calculate_rate_tab: Calculate FX rate
      convert_tab: Convert with FX rate
      date: Date
      destination_amount: Destination amount
      destination_amount_display: "Destination amount: %{amount}"
      exchange_rate: Exchange rate
      exchange_rate_display: "Exchange rate: %{rate}"
      exchange_rate_help: Choose how to enter the transfer amount.
      expense: Expense
      from: From
      accept_loan_payment_split: Accept split
      income: Income
      leave_loan_payment_unmatched: Leave unmatched
      loan_payment_extra_principal: Extra principal
      loan_payment_interest: Interest
      loan_payment_principal: Principal
      loan_payment_split_description: Only principal will reduce the loan balance; interest will be recorded as spending from the source account.
      loan_payment_split_title: Proposed loan payment split
      loan_payment_variance: Variance
      select_account: Select account
      source_amount: Source amount
      submit: Create transfer
      to: To
      transfer: Transfer
      update_loan_payment_split: Update preview
    new:
      title: New transfer
    show:
      delete: Remove transfer
      delete_subtitle: This removes the transfer.  It will not delete the underlying
        transactions.
      delete_title: Remove transfer?
      details: Details
      mark_recurring: Mark as recurring
      mark_recurring_subtitle: Track this transfer as a recurring pattern in the upcoming
        feed and recurring page.
      mark_recurring_title: Mark transfer as recurring
      note_label: Notes
      note_placeholder: Add a note to this transfer
      overview: Overview
      settings: Settings
      from: From
      to: To
      date: Date
      amount: Amount
      category: Category
      uncategorized: Uncategorized
    update:
      success: Transfer updated
