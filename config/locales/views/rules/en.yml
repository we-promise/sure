---
en:
  rules:
    no_action: No Action
    no_condition: No condition
    rule:
      edit: Edit
      re_apply_rule: Re-apply rule
      delete: Delete
      then: THEN
      and_more_conditions:
        one: and 1 more condition
        other: and %{count} more conditions
      and_more_actions:
        one: and 1 more action
        other: and %{count} more actions
      action_label_to: "%{label} to %{value}"
      all_past_and_future: All past and future %{resource}
      on_or_after: "%{resource} on or after %{date}"
    form:
      rule_name_label: Rule name (optional)
      rule_name_placeholder: Enter a name for this rule
      add_condition: Add condition
      add_condition_group: Add condition group
      add_action: Add action
      all_past_and_future: All past and future %{resource}
      starting_from: Starting from
      then: THEN
      if: IF
      for: FOR
      to: to
      back: Back to rules
    new:
      title: New transaction rule
    edit:
      title: Edit transaction rule
      title_named: Edit transaction rule "%{name}"
    apply:
      activated: Transaction rule activated
    primary_condition: "If %{filter} %{operator} %{value}"
    resource_types:
      transaction:
        one: transaction
        other: transactions
    operators:
      like: Contains
      eq: Equal to
      is_null: Is empty
      gt: Greater than
      gte: Greater or equal to
      lt: Less than
      lte: Less than or equal to
      neq: Is not equal to
    condition_filter_labels:
      transaction_name: Transaction name
      transaction_amount: Transaction amount
      transaction_type: Transaction type
      transaction_merchant: Transaction merchant
      transaction_category: Transaction category
      transaction_details: Transaction details
      transaction_notes: Transaction notes
      transaction_account: Transaction account
    action_executors:
      set_transaction_category: Set transaction category
      set_transaction_tags: Set transaction tags
      set_transaction_merchant: Set transaction merchant
      set_transaction_name: Set transaction name
      set_investment_activity_label: Set investment activity label
      exclude_transaction: Exclude from budgeting and reports
      set_as_transfer_or_payment: Set as transfer or payment
      send_email_notification: Send email notification
      auto_categorize: Auto-categorize transactions with AI
      auto_categorize_cost: "Auto-categorize transactions with AI (~$%{cost} per 20 transactions)"
      auto_categorize_cost_na: "Auto-categorize transactions with AI (cost: N/A)"
      auto_categorize_no_provider: Auto-categorize transactions with AI (no LLM provider configured)
      auto_detect_merchants: Auto-detect merchants
      auto_detect_merchants_paid: Auto-detect merchants with AI ($$)
    category_rule_cta:
      updated_to: Updated to %{category}
      description: You can create a rule to automatically categorize transactions like this one
      dont_show_again: Don't show this again
      dismiss: Dismiss
      create_rule: Create rule
    index:
      page_title: Rules
      delete_all_rules: Delete all rules
      new_rule: New rule
      ai_cost_warning: AI-enabled rule actions will cost money.  Be sure to filter as narrowly as possible to avoid unnecessary costs.
      rules_heading: Rules
      sort_by: "Sort by:"
      sort_name: Name
      sort_updated_at: Updated At
      toggle_sort_direction: Toggle sort direction
      no_rules_title: No rules yet
      no_rules_description: Set up rules to perform actions to your transactions and other data on every account sync.
    confirm:
      title: Confirm changes
      title_with_name: "Confirm changes to \"%{name}\""
      apply_notice_html: "You are about to apply this rule to <span class=\"text-primary font-medium\">%{count} %{resource}</span> that meet the specified rule criteria.  Please confirm if you wish to proceed with this change."
      ai_cost_title: AI Cost Estimation
      ai_cost_with_estimate_html: "This will use AI to categorize %{count} transaction(s). Estimated cost: <span class=\"font-semibold\">~$%{cost}</span>"
      ai_cost_no_estimate_html: "This will use AI to categorize %{count} transaction(s)."
      cost_unavailable_model: "Cost estimation unavailable for model \"%{model}\"."
      cost_unavailable_no_provider: Cost estimation unavailable (no LLM provider configured).
      cost_warning: You may incur costs, please check with the model provider for the most up-to-date prices.
      view_usage_history: View usage history
      confirm_changes: Confirm changes
    actions:
      value_placeholder: Enter a value
    update:
      success: Rule updated
    destroy:
      success: Rule deleted
    destroy_all:
      success: All rules deleted
    apply_all:
      button: Apply All
      confirm_title: Apply All Rules
      confirm_message: You are about to apply %{count} rules affecting %{transactions} unique transactions. Please confirm if you wish to proceed.
      confirm_button: Confirm and Apply All
      success: All rules have been queued for execution
      ai_cost_title: AI Cost Estimation
      ai_cost_message: This will use AI to categorize up to %{transactions} transactions.
      estimated_cost: "Estimated cost: ~$%{cost}"
      cost_unavailable_model: Cost estimation unavailable for model "%{model}".
      cost_unavailable_no_provider: Cost estimation unavailable (no LLM provider configured).
      cost_warning: You may incur costs, please check with the model provider for the most up-to-date prices.
      view_usage: View usage history
    recent_runs:
      title: Recent Runs
      description: View the execution history of your rules including success/failure status and transaction counts.
      unnamed_rule: Unnamed Rule
      columns:
        date_time: Date/Time
        execution_type: Type
        status: Status
        rule_name: Rule Name
        transactions_counts:
          queued: Queued
          processed: Processed
          modified: Modified
          blocked: Blocked
      execution_types:
        manual: Manual
        scheduled: Scheduled
      statuses:
        pending: Pending
        success: Success
        failed: Failed
    clear_ai_cache:
      button: Reset AI cache
      confirm_title: Reset AI cache?
      confirm_body: Are you sure you want to reset the AI cache? This will allow AI rules to re-process all transactions. This may incur additional API costs.
      confirm_button: Reset Cache
      success: AI cache is being cleared. This may take a few moments.
    condition_filters:
      transaction_type:
        income: Income
        expense: Expense
        transfer: Transfer
        equal_to: Equal to
  rule:
    conditions:
      condition_group:
        and_prefix: and
        match: match
        all: all
        any: any
        of_the_following_conditions: of the following conditions
        add_condition: Add condition
      condition:
        value_placeholder: Enter a value
