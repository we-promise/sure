---
en:
  activerecord:
    attributes:
      user:
        email: Email
        family: "%{moniker}"
        family_id: "%{moniker}"
        first_name: First Name
        last_name: Last Name
        password: Password
        password_confirmation: Password Confirmation
    errors:
      models:
        user:
          attributes:
            base:
              cannot_deactivate_admin_with_other_users: Admin cannot delete account
                while other users are present. Please delete all members first.
              provider_item_has_other_accounts: User owns accounts linked to a provider connection that also has accounts owned by another user. Move or unlink those accounts before changing family/group membership.
              cannot_remove_last_super_admin: Cannot remove the last active super
                admin. Promote another user to super admin first.
            profile_image:
              invalid_file_size: file size must be less than %{max_megabytes}MB
