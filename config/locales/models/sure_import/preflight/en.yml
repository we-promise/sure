---
en:
  sure_import:
    preflight:
      missing_reference: "Line %{line} %{type} references missing %{field} %{value}."
      skipped_missing_reference: "Line %{line} %{type} references missing %{field} %{value}. It will be skipped during import."
      skipped_missing_merchant_reference: "Line %{line} %{type} references a merchant (%{value}) that isn't included in this export. This can happen with an export taken before provider-assigned merchants were added to Sure exports. It will import without that merchant."
      skipped_unnamed_recurring: "Line %{line} %{type} has no name and references a merchant (%{value}) that isn't included in this export. It will be skipped during import."
      provider_merchant_diff: "Line %{line} ProviderMerchant %{name} already exists with a different %{fields}. The existing merchant will be kept."
