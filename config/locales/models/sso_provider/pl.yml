---
pl:
  activerecord:
    errors:
      models:
        sso_provider:
          attributes:
            settings:
              metadata_url_invalid: Adres URL metadanych IdP musi być prawidłowym
                adresem URL
              saml_cert_required: Certyfikat IdP lub odcisk palca certyfikatu jest
                wymagany, gdy nie używano adresu URL metadanych
              saml_url_required: Adres URL metadanych IdP lub adres URL SSO IdP jest
                wymagany dla dostawców SAML
              sso_url_invalid: Adres URL SSO IdP musi być prawidłowym adresem URL
