---
nb:
  recurring_transactions:
    allocator:
      missing_rate: ingen valutakurs fra %{from} til %{to}; angi et beløp manuelt
      unmeasurable: denne transaksjonen har en fordeling som ikke kan konverteres til %{currency}, så det som gjenstår av den kan ikke beregnes. Legg til en valutakurs for %{date} eller fjern den eksisterende betalingen først
      over_allocated: å fordele %{amount} overstiger transaksjonens gjenstående %{capacity} %{currency}
      exceeds_remaining: '%{amount} overstiger de gjenstående %{remaining} i denne perioden'
  activerecord:
    attributes:
      recurring_transaction:
        payment_url: Betalingslenke
    errors:
      models:
        recurring_transaction:
          merchant_or_name_required: Enten forhandler eller navn må være angitt
          attributes:
            payment_url:
              invalid_scheme: må være en gyldig http://- eller https://-adresse
            anchor_date:
              required_for_intervals: er påkrevd når en regel gjentas hver 2. periode eller sjeldnere
            bill_type:
              transfer_shape_mismatch: kan bare være overføring når en mottakerkonto er angitt
            expected_amount_min:
              greater_than_max: kan ikke være høyere enn forventet maksimumsbeløp
            account:
              required_for_transfer: må være angitt for en fast overføring
              wrong_family: må tilhøre denne husholdningen
            destination_account:
              same_as_source: kan ikke være den samme som fra-kontoen
              family_mismatch: må tilhøre samme husholdning som fra-kontoen
              wrong_family: må tilhøre denne husholdningen
