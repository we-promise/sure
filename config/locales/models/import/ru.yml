---
ru:
  activerecord:
    attributes:
      import:
        col_sep: Разделитель столбцов
        col_seps:
          comma: Запятая (,)
          semicolon: Точка с запятой (;)
        currency: Валюта
        number_format: Формат числа
    errors:
      models:
        import:
          attributes:
            raw_file_str:
              invalid_csv_format: не является допустимым форматом CSV
          duplicate_headers: 'Заголовки CSV нормализуются до дублирующихся столбцов:
            %{columns}'
