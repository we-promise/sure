---
ru:
  activerecord:
    attributes:
      import:
        currency: Валюта
        number_format: Формат чисел
    errors:
      models:
        import:
          attributes:
            raw_file_str:
              invalid_csv_format: не является корректным форматом CSV
