---
it:
  activerecord:
    errors:
      models:
        entry:
          attributes:
            base:
              invalid_sell_quantity: impossibile vendere %{sell_qty} azioni di %{ticker} perché
                ne possiedi solo %{current_qty}
