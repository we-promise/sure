---
en:
  activerecord:
    errors:
      models:
        category_import:
          own_parent: "Category '%{name}' cannot be its own parent"
          missing_columns: "Missing required columns: %{columns}"
