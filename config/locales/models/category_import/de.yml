---
de:
  activerecord:
    errors:
      models:
        category_import:
          missing_columns: 'Es fehlen Pflichtspalten: %{columns}'
          own_parent: Die Kategorie „%{name}“ kann nicht ihre eigene Oberkategorie sein
