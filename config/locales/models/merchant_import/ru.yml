---
ru:
  activerecord:
    errors:
      models:
        merchant_import:
          duplicate_columns: 'Повторяющиеся названия столбцов после нормализации: %{columns}'
          missing_columns: 'Отсутствуют обязательные столбцы: %{columns}'
