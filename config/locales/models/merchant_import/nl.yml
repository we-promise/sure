---
nl:
  activerecord:
    errors:
      models:
        merchant_import:
          missing_columns: 'Verplichte kolommen ontbreken: %{columns}'
          duplicate_columns: 'Dubbele kolomnamen na normalisatie: %{columns}'
