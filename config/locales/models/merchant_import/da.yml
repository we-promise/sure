---
da:
  activerecord:
    errors:
      models:
        merchant_import:
          missing_columns: "Mangler påkrævede kolonner: %{columns}"
          duplicate_columns: "Dubletkolonnenavne efter normalisering: %{columns}"
