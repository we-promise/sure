---
es:
  activerecord:
    attributes:
      goal:
        name: Nombre
        target_amount: Importe objetivo
        currency: Moneda
        target_date: Fecha objetivo
        color: Color
        notes: Notas
        state: Estado
        linked_accounts: Cuentas vinculadas
    errors:
      models:
        goal:
          attributes:
            base:
              at_least_one_linked_account_required: Elige al menos una cuenta para financiar este objetivo.
              whole_account_conflict_on_restore: 'No se puede restaurar este objetivo: «%{account_name}» ahora está reservada por completo para «%{goal_name}». Introduce primero un importe en uno de los dos.'
            linked_accounts:
              must_be_fundable: Todas las cuentas vinculadas deben ser cuentas de efectivo o de inversión.
              currency_mismatch: Todas las cuentas vinculadas deben compartir la misma moneda.
              must_belong_to_family: Las cuentas vinculadas deben pertenecer a la misma familia que el objetivo.
            currency:
              locked_after_linked: No se puede cambiar la moneda cuando el objetivo ya está vinculado a cuentas.
            target_mode:
              months_requires_maintained: Solo una reserva que mantienes puede tener un objetivo expresado en meses de gastos.
            target_months:
              blank: Indica cuántos meses de gastos debe cubrir esta reserva.
              only_with_months_mode: Los meses de gastos solo se aplican cuando el objetivo se define de esa forma.
            kind:
              locked_while_released: Reabre este objetivo antes de convertirlo en una reserva.
              locked_once_consumed: Este objetivo ya ha registrado dinero como gastado, por lo que no puede convertirse en una reserva.
            target_amount:
              below_consumed: 'No se puede fijar una meta inferior a lo que este objetivo ya ha registrado como gastado.'
