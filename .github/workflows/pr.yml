name: Pull Request

on:
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize, reopened]
    paths-ignore:
      - 'charts/**'

permissions:
  contents: read

jobs:
  ci:
    uses: ./.github/workflows/ci.yml
