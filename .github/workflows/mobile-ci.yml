name: Mobile CI

# Disabled: automatic PR/push triggers removed. Runs manually only.
on:
  workflow_dispatch:

concurrency:
  group: mobile-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    uses: ./.github/workflows/flutter-build.yml
    secrets: inherit
