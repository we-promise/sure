<%# locals: (chat:, message: t("chats.thinking")) -%>

<div id="thinking-indicator" class="flex items-start gap-3">
  <%= render "chats/ai_avatar" %>
  <p class="text-sm text-secondary animate-pulse"><%= message %></p>
</div>
