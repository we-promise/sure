<div class="max-w-xl mx-auto">
  <div class="mb-6">
    <%= link_to saving_goals_path, class: "group flex items-center gap-1 text-sm text-subdued hover:text-primary mb-2 inline-block transition-colors" do %>
      <%= icon "chevron-left", class: "w-4 h-4" %>
      <span><%= t("common.back") %></span>
    <% end %>
    <h1 class="text-2xl font-bold text-primary"><%= t("saving_goals.new.title") %></h1>
  </div>

  <div class="bg-container rounded-xl shadow-border-xs p-6">
    <%= render "form", saving_goal: @saving_goal %>
  </div>
</div>
