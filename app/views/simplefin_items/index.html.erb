<%= content_for :page_title, t("simplefin_items.index.page_title") %>

<div class="space-y-4">
  <% if @simplefin_items.any? %>
    <% @simplefin_items.each do |simplefin_item| %>
      <%= render simplefin_item %>
    <% end %>
  <% else %>
    <div class="bg-container rounded-xl shadow-border-xs p-4">
      <div class="flex justify-center items-center py-20">
        <div class="text-center flex flex-col items-center max-w-[300px]">
          <p class="text-primary mb-1 font-medium text-sm"><%= t("simplefin_items.index.empty_state.title") %></p>
          <p class="text-secondary text-sm mb-4"><%= t("simplefin_items.index.empty_state.description") %></p>
          <%= render DS::Link.new(
            text: t("simplefin_items.index.empty_state.add_connection"),
            icon: "plus",
            variant: "primary",
            href: new_simplefin_item_path,
            frame: :modal
          ) %>
        </div>
      </div>
    </div>
  <% end %>
</div>
