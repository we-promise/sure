<%= turbo_frame_tag "modal" do %>
  <%= render DS::Dialog.new do |dialog| %>
    <% dialog.with_header(title: t(".title", account_name: @account.name)) %>

    <% dialog.with_body do %>
      <div class="space-y-4">
        <% if @api_error.present? %>
          <%= render DS::Alert.new(message: @api_error, variant: :error) %>
        <% elsif @monobank_accounts.empty? %>
          <p class="text-sm text-secondary"><%= t(".no_accounts_found") %></p>
        <% else %>
          <p class="text-sm text-secondary"><%= t(".description") %></p>

          <%= form_with url: link_existing_account_monobank_items_path,
                        method: :post,
                        data: { turbo_frame: "_top" },
                        class: "space-y-4" do %>
            <%= hidden_field_tag :monobank_item_id, @monobank_item.id %>
            <%= hidden_field_tag :account_id, @account.id %>
            <%= hidden_field_tag :return_to, @return_to %>

            <div class="space-y-2">
              <% @monobank_accounts.each do |monobank_account| %>
                <label class="flex items-start gap-3 p-3 border border-primary rounded-lg hover:bg-surface cursor-pointer transition-colors">
                  <%= radio_button_tag "monobank_account_id", monobank_account.id, false, class: "mt-1", required: true %>
                  <div class="flex-1">
                    <div class="font-medium text-sm text-primary"><%= monobank_account.name %></div>
                    <div class="text-xs text-secondary mt-1">
                      <%= [monobank_account.type_label, monobank_account.currency].compact_blank.join(" · ") %>
                    </div>
                  </div>
                </label>
              <% end %>
            </div>

            <div class="flex gap-2 justify-end pt-4">
              <%= render DS::Link.new(
                text: t(".cancel"),
                href: @return_to || accounts_path,
                variant: :secondary,
                data: { turbo_frame: "_top", action: "DS--dialog#close" }
              ) %>
              <%= render DS::Button.new(text: t(".link_account"), type: "submit") %>
            </div>
          <% end %>
        <% end %>
      </div>
    <% end %>
  <% end %>
<% end %>
