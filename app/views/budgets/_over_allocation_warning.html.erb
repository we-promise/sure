<%# locals: (budget:) %>

<div class="flex flex-col gap-4 items-center justify-center h-full">
  <%= icon "alert-triangle", size: "lg", color: "destructive" %>
  <p class="text-secondary text-sm text-center"><%= t("budgets.over_allocation.message") %></p>

  <%= render DS::Link.new(
    text: t("budgets.over_allocation.fix"),
    variant: "secondary",
    size: "sm",
    icon: "pencil",
    icon_position: "right",
    href: budget_budget_categories_path(budget)
  ) %>
</div>
