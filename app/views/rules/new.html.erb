<%= link_to t("rules.new.back_to_rules"), rules_path %>

<%= render DS::Dialog.new do |dialog| %>
  <% dialog.with_header(title: t("rules.new.new_rule_title", resource_type: @rule.resource_type)) %>
  <% dialog.with_body do %>
    <%= render "rules/form", rule: @rule %>
  <% end %>
<% end %>
